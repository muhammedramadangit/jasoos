import 'package:flutter/material.dart';
import 'package:jasoos/main_widgets/custom_button.dart';

class SelectCategorySubmit extends StatelessWidget {
  const SelectCategorySubmit({super.key});

  @override
  Widget build(BuildContext context) {
    return CustomButton(
      text: "Next",
      onTap: () {},
    );
  }
}
